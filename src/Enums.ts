namespace curve {
    export enum FillRule {
        EvenOdd = 0,
        NonZero = 1
    }
    export enum SweepDirection {
        Counterclockwise = 0,
        Clockwise = 1,
    }
    export enum PenLineCap {
        Flat = 0,
        Square = 1,
        Round = 2,
        Triangle = 3,
    }
    export enum PenLineJoin {
        Miter = 0,
        Bevel = 1,
        Round = 2,
    }
}